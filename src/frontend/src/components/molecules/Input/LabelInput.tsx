import styled from "styled-components";

const LabelInput = () => {
  return <LabelInputContainer>input</LabelInputContainer>;
};

const LabelInputContainer = styled.div`
  margin-bottom: 20px;
`;

export default LabelInput;
